# Boniface — חוקי בנייה (Rules)

**גרסה:** 1.0 · אוגוסט 2026  
**מחייב עבור:** מפתחים · סוכני AI · Code Review  
**אפיון מוצר:** [`spec.md`](spec.md)

---

## 0. כלל-על

1. **Boniface היא אפליקציית מובייל בלבד** (iOS + Android דרך Expo/EAS → App Store + Google Play).
2. **אין לבנות מוצר ווב ללקוח.** דפדפן מותר רק ל: צפייה ב-GitHub בזמן פיתוח, דפי מדיניות לחנויות, כלי פנימי לצוות.
3. האפיון ב-`spec.md` גובר על הקוד הישן. בבנייה מאפס מותר לשכתב — אסור לשבור את החוזה המוצרי.
4. אין ship של כפתורים מתים, stubs, או `implemented: true` על פיצ'ר שלא עובד.

---

## 1. סטאק — מה מותר / אסור

### מותר (ברירת מחדל)
| שכבה | בחירה |
|------|--------|
| Mobile | Expo SDK 54+, expo-router, React Native |
| שרת | Express 5 + TypeScript |
| DB | PostgreSQL + Drizzle |
| ולידציה | Zod בכל גבול API וטפסים קריטיים |
| Client API | OpenAPI → Orval → TanStack Query |
| מונוריפו | pnpm workspaces |
| אחסון מקומי | AsyncStorage (או החלפה מוצדקת ל-SQLite אם נדרש לביצועים) |
| Build | EAS Build / Submit |

### אסור בלי אישור מפורש באפיון
- Next.js / Remix / אפליקציית ווב כלקוח
- Firebase Auth / Supabase כתחליף מלא לארכיטקטורה (אלא אם יוחלט מחדש באפיון)
- Purple-on-white / cream+serif AI-default aesthetics
- Light mode
- POS integration לפני סוף P0–P1 לפי roadmap

---

## 2. מבנה מונוריפו (יעד)

```
boniface/
├── apps/
│   └── mobile/          # Expo app — המוצר היחיד למשתמש
├── packages/
│   ├── api-client/      # Orval generated
│   └── shared/          # Zod schemas, types משותפים
├── server/              # Express API + Drizzle
├── spec.md
├── rules.md
└── improvments.md
```

במעבר מה-baseline הנוכחי (תיקיית mobile שטוחה) — לשמור על התנהגות האפיון; מבנה התיקיות יכול להתיישר בהדרגה.

---

## 3. שכבות קוד באפליקציה

```
UI (app/, components/)
  → hooks / contexts
    → TanStack Query / api-client
      → HTTPS API
        → Express routes
          → Drizzle / PostgreSQL
```

**כללים:**
- מסכים לא מדברים ישירות ל-fetch גולמי אם קיים hook מ-Orval.
- לוגיקת חישוב טיפים / מגבלות משמרת — ב-`shared` או מודול טהור עם טסטים; לא מוטמעת עמוק ב-JSX.
- Side-effects (IAP, Push, Share) מאחורי ממשק ברור — קל למוק בטסטים.

---

## 4. Offline-first

1. פעולות ליבה (משמרת, טיפים, סטופ-ליסט, צ'ק-ליסט) **חייבות** לעבוד בלי רשת.
2. כל ישות מסונכרנת: כתיבה קודם ל-local store, אחר כך queue לסנכרון.
3. כישלון סנכרון: לא מוחקים נתונים מקומיים; מציגים סטטוס «ממתין לסנכרון».
4. אחרי login: hydrate מהשרת בזהירות — לא לדרוס שינויים מקומיים שלא סונכרנו.

---

## 5. Auth ותפקידים

| תפקיד | גישה |
|-------|------|
| Owner / Manager (UP) | מלא לפי מנוי |
| Employee | מסכים מצומצמים בלבד |

- PIN / סיסמאות: hashing בשרת בלבד; לא plaintext ב-AsyncStorage.
- עובד לעולם לא רואה: מחיקת צוות, הגדרות מנוי, החלפת מקום (אלא אם הוגדר במפורש), עריכת מחירי Feature Cards.
- שחזור עובד — רק דרך UP.

---

## 6. i18n ו-RTL

1. **אין מחרוזות UI קשיחות** בקוד — רק מפתחות בתרגומים (`he` / `ru` / `en`).
2. כשמוסיפים מסך/מודל — מוסיפים תרגום לשלוש השפות באותו PR/שינוי.
3. `he` ⇒ RTL מלא (layout, אייקונים כיווניים, תאריכים).
4. פורמט מטבע לפי `venue.currency` (ברירת מחדל ILS).

---

## 7. עיצוב (Design System)

| Token | ערך |
|-------|------|
| Background | `#111827` |
| Primary / Accent | `#F59E0B` |
| Font | Inter |
| Mode | Dark only |

**חובה:**
- Haptics בפעולות משמעותיות (פתיחת/סגירת משמרת, שמירה, שגיאה).
- Keyboard-aware בכל טפסים; אסור שמקלדת תכסה שדה פעיל.
- רשימות ארוכות תמיד עם Scroll (ראה באג StartShiftModal).
- Tab Quick במרכז נשאר ה-hub התפעולי.

**אסור:**
- כרטיסים מיותרים ב-hero / דשבורד עמוס.
- צבעי neon glow סגול כברירת מחדל.
- שינוי פלטת המותג בלי עדכון אפיון.

---

## 8. Feature Cards ומנוי

1. כרטיס `implemented: true` **רק** אחרי שיש מסלול משתמש מלא באפליקציה.
2. נעילת פרימיום לפי סטטוס מנוי מהשרת (לא רק toggle מקומי בפרודקשן).
3. IAP: אימות קבלה בשרת מול Apple/Google לפני פתיחת גישה.
4. התנהגות מנוי שפג — לפי `spec.md` §7.3 (UP מיד; עובדים +חודש; מחיקה אחרי 3 חודשים).

---

## 9. API ושרת

1. כל route עם Zod input/output.
2. שגיאות אחידות: `{ error: string, code?: string }`.
3. Auth Bearer על כל route פרטי.
4. אין secrets ב-repo; `.env` ב-gitignore.
5. מיגרציות Drizzle לכל שינוי סכמה — לא «תיקון ידני בפרודקשן».
6. OpenAPI מעודכן → regenerate Orval לפני שימוש בצד מובייל.

---

## 10. איכות קוד

| כלל | פירוט |
|-----|--------|
| TypeScript | strict; אין `any` חדש בלי הצדקה |
| קומפוננטות | מסך דק; לוגיקה ב-hooks/utils |
| שגיאות | ErrorBoundary ברמה הגבוהה; fallback ברור |
| לוגים | לא מדפיסים PII / PIN / tokens |
| טסטים | חובה לחישוב טיפים, מגבלות משמרות, לוגיקת מנוי |
| מתים | למחוק מסכי legacy (`employees` הכפול וכו') במקום להשאיר |

---

## 11. אבטחה ופרטיות (PDPA)

1. מינימום נתונים: לא לאסוף מה שלא צריך.
2. מחיקת חשבון / מחיקה אוטומטית אחרי 3 חודשי מנוי מת — מיושמת בשרת.
3. ייצוא נתונים לפי בקשה (לפחות CSV/JSON ל-UP).
4. מדיניות פרטיות ותנאי שימוש — נגישים **בתוך האפליקציה** + URL ציבורי לדרישות החנויות.
5. גיבוי / שחזור מכשיר — דרך חשבון ענן, לא דרך ווב-אפ.

---

## 12. תהליך פיתוח לפי Roadmap

1. עובדים לפי שלבים: **P0 → P1 → P2 → P3** כפי שב-`spec.md`.
2. לא מתחילים P2/P3 לפני ש-P0 יציב לחנויות (אלא spike מוגבל ומסומן).
3. כל Epic גדול מתחיל מעדכון קצר ל-`spec.md` אם יש שינוי התנהגות.
4. באגים מ-`improvments.md` בסוף הקובץ — עדיפות P0.

---

## 13. Git ו-PR

1. לא קומיטים של `.env`, מפתחות, keystores.
2. הודעות קומיט בעברית או אנגלית — ברורות, מתמקדות ב-why.
3. לא לדחוף ל-`main` פיצ'ר שבור / stub גלוי למשתמש.
4. לפני Submit לחנות: checklist מ-`spec.md` §16 (חנויות).

---

## 14. EAS וחנויות

1. פרופילי EAS נפרדים: development / preview / production.
2. Android: AAB לפרודקשן; APK ל-preview פנימי.
3. iOS: TestFlight לפני Production.
4. גרסאות `version` + `buildNumber`/`versionCode` עולים בכל submit.
5. אחרי שפיצ'ר מוכן — **מעלים לחנויות**, לא «מפרסמים באתר».

---

## 15. סוכני AI — הוראות מיוחדות

כשסוכן בונה או משנה קוד:

1. לקרוא `spec.md` + `rules.md` לפני שינוי ארכיטקטוני.
2. לא להציע «בואו נעשה גם דשבורד ווב» — זה מחוץ לסקופ.
3. לא לסמן Feature Card כ-implemented בלי flow מלא.
4. כל UI חדש: תרגום he/ru/en + RTL.
5. כל טופס: keyboard-aware + ולידציה.
6. להעדיף תיקון לפי האפיון על פני העתקה עיוורת של קוד legacy.
7. דיאגרמות/מסמכים ב-GitHub הם לעיניים אנושיות — לא תחליף לאפליקציה.

---

## 16. הגדרת «100% מוכן»

פיצ'ר נחשב מוכן רק אם:

- [ ] עובד ב-iOS ו-Android (או נבדק בביטחון סביר בשניהם)
- [ ] עובד offline אם הוא חלק מהליבה
- [ ] מתורגם he/ru/en
- [ ] מכוסה באפיון / לא סותר אותו
- [ ] אין stub
- [ ] שגיאות מטופלות
- [ ] (אם פרימיום) נעול/פתוח לפי מנוי אמיתי

---

*אם כלל כאן מתנגש עם נוחות פיתוח קצרת-טווח — הכלל והאפיון מנצחים.*

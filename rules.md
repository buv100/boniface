# Boniface — חוקי בנייה (Rules)

**גרסה:** 2.1 · אוגוסט 2026  
**מחייב עבור:** מפתחים · סוכני AI · Code Review  
**אפיון:** [`spec.md`](spec.md) · **יומן שינויים:** [`change.md`](change.md)

כל שינוי בקוד, במודל או בהתנהגות מוצר **חייב** לעדכן את הקובץ הזה כשהחוזה המוצרי משתנה, ואת `change.md` תמיד.

---

## 0. כלל-על

1. Boniface היא מערכת תפעול **לרשתות/עסקים** של ברים ומסעדות. ארבעה צדדים לוגיים, **דשבורד נפרד** לבעלים / מנהל משמרת / עובד. צוות הפלטפורמה לא נמכר ללקוח.
2. **שלב נוכחי (חובה):** צד **בעל העסק / רשת** + **פאנל פלטפורמה** (אנחנו). מנהל משמרת ועובד מוקפאים — לא לחבר אותם לנווט החדש עד שלב ייעודי.
3. אפיון ב-`spec.md` גובר על קוד ישן. טבלאות משמרת/טיפים **לא נמחקות**, אבל אסור לבנות עליהן את דשבורד הבעלים.
4. אין ship של כפתורים מתים או `implemented: true` על פיצ'ר שלא עובד.
5. **כל שינוי** נרשם ב-[`change.md`](change.md): תאריך · מה השתנה · למה · איפה בקוד · איפה באפליקציה.
6. כשמוסיפים ישות, הרשאה, סוג חריגה, או צד משתמש חדש — **מעדכנים את `rules.md` ואת `spec.md` באותו שינוי**.
7. **מכירה:** אין הרשמת בעלים ציבורית. Boniface יוצרת את הלקוח. דשבורד בעלים רק כשמנוי הארגון `active` ולא פג תוקף.

---

## 1. ארבעת הצדדים

| צד | תפקיד | סטטוס |
|----|--------|--------|
| **פלטפורמה (Boniface)** | יוצרת לקוחות, מסמנת שולם/השהיה | **בבנייה** · JWT `platform_admin` |
| **בעל עסק / רשת (Owner)** | מתחבר ב-PIN, רואה דשבורדים אחרי תשלום | **בבנייה** |
| **מנהל / אחראי משמרת** | מפעיל משמרת חיה בסניף | מוקפא |
| **עובד** (ברמן / מלצר / טבח…) | דשבורד אישי מצומצם | מוקפא |

- Owner = **ארגון אחד** → **N סניפים**. אסור לזהות בעלים עם «בר אחד + טלפון יחיד» כמו ב-baseline הישן.
- כניסה לרשת: רשימת סניפים. **לפני בחירת סניף** מוצגות חריגות לכל סניף.
- **הגדרת מה נחשב לחריגה בכל נושא עדיין לא נקבעה.** חובה לשמור `VenueAlert[]` בכל סניף (ריק עד שיוגדרו כללים). לא לנחש כללי חריגה.
- מנוי חי על **הארגון** (`org_subscriptions`), לא על סניף. טבלת `subscriptions` הישנה (לפי `venue_id`) נשארת למשמרת ולא חוסמת דשבורד בעלים.

---

## 2. מכירה ו-onboarding

אין self-signup לבעלים. `POST /auth/owner/register` סגור (403 `NO_PUBLIC_SIGNUP`).

1. צוות Boniface נכנס ל-`/admin/login` (לא מופיע במסך הבעלים).
2. יוצרים לקוח: פרטי בעלים + עסק + סניף ראשון + PIN זמני + «שולם עד».
3. הבעלים רואה **כניסה בלבד** (טלפון + PIN). בלי כפתור הרשמה.
4. אם המנוי לא `active` או התאריך עבר: מסך חסום + התנתקות. בלי קישורים לדשבורד.
5. אם פעיל: hub / דשבורד סניף כמו היום.

**תשלום v1 = סימון ידני** אחרי העברה/חשבונית (`expires_at`, `status`, `notes`). אין Stripe / Cardcom / IAP / פורטל תשלום ללקוח. סליקה אוטומטית = שלב נפרד בלי לשנות את שער הגישה.

סטטוס מנוי: `active` | `past_due` | `suspended`. שער: `requirePaidOrg` על כל `/api/owner/*` ועל `POST /auth/select-venue`. כשל → **402** `SUBSCRIPTION_INACTIVE`. כניסת בעלים (`/auth/owner/login` + `/auth/me`) נשארת פתוחה כדי להציג את מסך החסימה.

---

## 3. דשבורד בעלים (סניף)

חובה במסך הבית:

- שלום + שם הבעלים
- תאריך ושעה חיים
- שם הסניף הפעיל (לחיצה → בחירת סניף / רשת)
- כרטיס לכל נושא — **מסך CRUD אמיתי**, לא stub:
  - דשבורד כספי: הכנסות / הוצאות / רווח לחודש (הזנה ידנית)
  - סידור עבודה: צפייה בשבוע + הוספת משמרת לעובד
  - עובדים
  - מלאי (בר ומטבח באותו מסך, שני אזורים) — כולל עלות ליחידה
  - תפריט בר / מטבח (עץ BOM + עלות מחושבת)
  - ספקים
  - הגדרות סניף/עסק

### עובדים (כרטיס HR, לא משמרת)

- תפקיד: ברמן / מלצר / טבח + אפשרות להוסיף תווית
- הרשאות (JSON flags) שהבעלים מגדיר עכשיו; ייאכפו כשנבנה צד מנהל/עובד
- שכר: `hourly` | `monthly` (גלובלי) | `topup` (השלמה)
- ת.ז, טופס 101, מסמכים נוספים

הרשאות ברירת מחדל (מפתחות יציבים, לא מחרוזות UI):

`view_stock` · `edit_stock` · `manage_staff` · `manage_recipes` · `manage_suppliers` · `run_shift` · `view_reports`

### מלאי

- `department`: `bar` | `kitchen` בלבד
- בר ≠ מטבח. לא לערבב קטגוריות.
- סף מינימום: סימון ויזואלי במסך המלאי. **לא** לחבר לחריגות רשת עד שיוגדרו כללים.
- `unitCost` — מחיר קנייה ליחידה, לחישוב עלות מתכון.

### תפריט (BOM)

- מוצר (קוקטייל / סירופ ביתי / קישוט / מנה / תבשיל / רוטב…) → שורות: מלאי **או** מתכון-בן + כמות.
- עלות מנה = סכום (כמות × `unitCost` של מלאי, או עלות מתכון-בן). אין ניכוי אוטומטי ממלא.

### ספקים

- שם, טלפון, מה מספק, מתי (ימים/הערה), הערות
- קישור אופציונלי לפריטי מלאי; אם משויך — אפשר להראות מה חסר אצל הספק הזה (מתחת לסף)

---

## 4. סטאק (שלב 1)

| שכבה | בחירה |
|------|--------|
| לקוח | Expo SDK 54+, expo-router, React Native + **Web** ([boniface.expo.app](https://boniface.expo.app)) |
| שרת | Express + TypeScript |
| DB | SQLite + Drizzle + better-sqlite3 |
| ולידציה | Zod בגבולות API |

**אסור בלי עדכון אפיון:** Next.js נפרד, Firebase/Supabase כ-auth ראשי, POS, Postgres (יעד עתידי, לא עכשיו).

ערוצי הפצה: EAS (iOS/Android) + Expo web. אין מוצר ווב נפרד מ-Expo.

---

## 5. Auth

| תפקיד JWT | נתיב UI |
|-----------|---------|
| `platform_admin` | `app/admin/*` |
| `owner` | `app/owner/*` (או `blocked` אם המנוי לא פעיל) |
| `manager` | קוד ישן; לא לשלוח לדשבורד בעלים |
| `employee` | `app/employee/*` (מוקפא) |

- PIN / סיסמה: hashing בשרת בלבד.
- Session בעלים נושא `ownerId` + `organizationId` + `venueId` (הסניף הפעיל) בטבלת `sessions`.
- Session אדמין בטבלה נפרדת `admin_sessions` (אין `venue_id`).
- בחירת סניף מנפיקה session מעודכן — לא רק state מקומי — ורק עם מנוי פעיל.
- אדמין ראשון: bootstrap מ-env `PLATFORM_ADMIN_PHONE` + `PLATFORM_ADMIN_PIN` (ברירת מחדל לבדיקה: `0501234567` / `2020`). מעדכן גם אם כבר יש אדמין.
- בעלים לבדיקה מקומית: `DEV_LOGIN_PHONE` + `DEV_LOGIN_PIN` — אותו טלפון/PIN, מנוי פעיל לשנה.

---

## 6. i18n ו-RTL

1. אין מחרוזות UI קשיחות — רק מפתחות ב-`lib/translations.ts` (`he` / `ru` / `en`).
2. מסך חדש = תרגום לשלוש השפות באותו שינוי.
3. `he` ⇒ RTL מלא.
4. מטבע ברירת מחדל ILS.

---

## 7. עיצוב

| Token | ערך |
|-------|------|
| Background | `#111827` |
| Primary / Accent | `#F59E0B` |
| Font | Inter |
| Mode | Dark only |

דשבורד בעלים: כרטיסי נושא גדולים, לא חמשת טאבי המשמרת. Haptics בפעולות שמירה/מחיקה.

---

## 8. מבנה קוד (שלב בעלים + פלטפורמה)

```
app/owner/          # דשבורד בעלים (כולל blocked)
app/account.tsx     # כניסת בעלים בלבד
app/admin/          # פאנל Boniface
server/src/routes/owner.ts
server/src/routes/admin.ts
server/src/db/schema.ts   # owners, organizations, org_subscriptions, platform_admins, …
```

מסכים לא קוראים ל-SQLite ישירות. כתיבה דרך API.

---

## 9. מה אסור לבנות עכשיו

- דשבורד מנהל משמרת / עובד (טיפים, נוכחות רבע-שעה, סטופ-ליסט חי)
- כללי חריגה סופיים (רק `VenueAlert[]`)
- קופה / POS / IAP / Stripe / Cardcom / פורטל תשלום ללקוח
- Orval / Postgres
- הרשמת בעלים ציבורית

---

## 10. עדכון החוקים (חובה)

אחרי כל פיצ'ר שמשנה התנהגות מוצר:

1. רשומה ב-`change.md`
2. אם נוסף תפקיד, ישות, הרשאה, סוג מלאי/שכר, או כלל חריגה — עדכון `rules.md` + `spec.md`
3. לא להשאיר את המסמכים מאחורי הקוד

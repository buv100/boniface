# Boniface local API
JWT_SECRET=change-me-in-production
PORT=3001

# Groq assistant (server-side only — do not put real keys in git)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Optional mobile override (physical device)
# EXPO_PUBLIC_API_URL=http://192.168.x.x:3001/api
